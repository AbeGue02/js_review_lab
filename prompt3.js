const colors = ['red', 'orange', 'yellow', 'green', 'blue']

console.log(colors[0])
console.log(colors[colors.length - 1])

colors.push('purple')
colors.unshift('pink')

console.log(colors)

colors.pop()
colors.shift()

console.log(colors)
